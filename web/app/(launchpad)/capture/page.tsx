import { AppPlaceholder } from "@/components/AppPlaceholder";

export default function Page() {
  return (
    <AppPlaceholder
      title="Capture"
      description="Capture management for federal opportunity pipelines."
    />
  );
}
